import statistics
dia_mean = [0.0]7
    dia_stdev = [0.0]7
    dia_count = [0]*7

    data = {}

    for line in sys.stdin:
      dia, cost = line.strip().split("\t")
      dia = int(dia)
      cost = float(cost)
     print(dia,cost)


/-----------------------------
if weeks in data:
        data[weeks].append(cost)
        print(data)
